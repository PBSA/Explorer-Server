var fonts = document.createElement('link');
fonts.setAttribute('rel', 'stylesheet');
fonts.setAttribute('href', '//fonts.googleapis.com/css?family=Barlow:300,300i,400,400i,600,600i');

// Append the new link tag to the end of the head.
document.head.appendChild(fonts);
